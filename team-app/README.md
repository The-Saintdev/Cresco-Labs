# Cresco Labs Team Mobile

The member-facing mobile app. It mirrors the team web app and includes:

- Model studio
- Text, image, and video workspaces
- History
- Usage and spend visibility
- Personal settings

This app must never contain provider API keys or admin-only model configuration.

Set `EXPO_PUBLIC_API_URL` to the same deployed Worker URL used by the admin app before an EAS build.
